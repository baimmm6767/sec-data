from pathlib import Path

requirements = """# sec-data dependencies
# Recommended Python version: 3.10 or newer
#
# Install all repository dependencies with:
#   python3 -m pip install -r requirements.txt
#
# Windows:
#   py -m pip install -r requirements.txt


# ── Core SEC extraction engine ──────────────────────────────────────────────

# HTTP client used for SEC filing and taxonomy requests
httpx

# Financial-data processing and numerical operations
pandas
numpy

# SEC EDGAR filing retrieval and XBRL access
# Installed package name: edgartools
# Imported in Python as: edgar
edgartools

# HTML filing and table parsing
beautifulsoup4
lxml

# Terminal progress interface
tqdm


# ── Optional extraction features enabled by default ─────────────────────────

# XBRL taxonomy, presentation, calculation, and definition-linkbase enrichment
# This may be omitted only when every extraction is run with --no-arelle.
arelle-release

# Excel workbook export used by --xlsx
# Without this package, sec-data falls back to CSV output.
openpyxl


# ── Optional HTML application proxy ─────────────────────────────────────────

# Required only for proxy.py and the browser-based financial-analysis app.
# The core sec_data.py extraction engine does not require these packages.
Flask
requests


# The JavaScript libraries used by the optional HTML application are loaded
# from CDNs in index.html and are therefore not installed through pip.
# External data features in the HTML app may also require their own API keys.
"""

path = Path("/mnt/data/requirements_updated.txt")
path.write_text(requirements, encoding="utf-8")
print(path)
print(requirements)
